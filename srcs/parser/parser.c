#include "minishell.h"


// ${VAR} == $VAR <----- access var
// $(VAR) == VAR  <----- execute cmd


// static  *t_lst
char *clean_command_line(char *line_buff)
{
    char *line_clean;
    
    return (line_clean);
}